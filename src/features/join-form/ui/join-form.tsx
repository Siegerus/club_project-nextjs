import { Input } from "@/shared/ui";

/* type JoinFormProps = {}; */

const JoinForm = (/* {}: JoinFormProps */) => {
  return (
    <form>
      <Input />
      <Input />
      <Input />
    </form>
  );
};

export default JoinForm;
