export type CardData = {
  titleText: {
    top: string;
    bottom?: string;
  };
  imageSrc: string;
  imageAlt: string;
  description: string;
};

export type Sizes = {
  width: number;
  height: number;
};
